import { Data } from "@angular/router";

export interface Person
{
    id: string;
    name: string;
    surname: string;
    birthPlace: string;
    province: string;
    gender: string;
    birthDate: string;
    taxCode: string;
}